{{!--
  file: ./templates/partials/navigation.hbs
  Pattern 3: Primary Navigation Bar
  
  Description:
  Horizontal navigation bar with main menu items.
  Displays below the app header in the sticky section.
  
  Configuration Source: _menu.json → primaryNavigation
  
  Features:
  - Horizontal layout with centered alignment
  - Active page highlighting (white background, black text)
  - Hover states (semi-transparent white)
  - Black background (#000000) with white text
  - Responsive (collapses to hamburger drawer on mobile)
  - Links to internal pages using linkPath helper
  
  Navigation Data:
  - Passed from renderer as primaryNav array
  - Each item includes: id, label, href, isActive
  
  Usage in page.hbs:
  {{> navigation primaryNav=primaryNav}}
--}}

<nav id="ips-main-menu" class="main-menu-bar" role="navigation" aria-label="Main Navigation">
  {{#if primaryNav}}
    {{#each primaryNav}}
      <a href="{{href}}" 
         class="main-menu-item {{#if isActive}}active{{/if}}" 
         data-nav-id="{{id}}"
         {{#if isActive}}aria-current="page"{{/if}}>
        {{label}}
      </a>
    {{/each}}
  {{else}}
    {{!-- Fallback: No navigation items found --}}
    <span class="main-menu-item" style="opacity: 0.5;">Navigation not configured</span>
  {{/if}}
</nav>
